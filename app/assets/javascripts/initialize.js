// This file contains all the initializations needed globally accross the site
// e.g. the menu.

$(document).ready(function() {
  'use strict';
  var menu = new mlPushMenu( document.getElementById( 'mp-menu' ), document.getElementById( 'trigger' ) );
});
